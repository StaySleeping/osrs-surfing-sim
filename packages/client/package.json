{
  "name": "@osrs-surfing/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "playtest": "playwright test e2e/gameplay.spec.ts e2e/playthrough.spec.ts",
    "playtest:update": "playwright test e2e/playthrough.spec.ts --update-snapshots",
    "visual:test": "playwright test e2e/",
    "visual:update": "playwright test e2e/ --update-snapshots"
  },
  "dependencies": {
    "@osrs-surfing/engine": "workspace:*",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "vite": "^6.2.2"
  }
}
